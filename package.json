{
  "name": "basex-online-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ basex-online-subgraph",
    "create-local": "graph create --node http://localhost:8020/ basex-online-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ basex-online-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 basex-online-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.21.0",
    "@graphprotocol/graph-ts": "^0.20.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
